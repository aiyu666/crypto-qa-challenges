# crypto-qa-challenges

# Installation
1. Install poetry
    ```
    brew install poetry
    ```
2. cd to project
    ```
    cd challenges_API
    ```
    or
    ```
    challenges_app_UI
    ```
3.  Install the dependency package
    ```
    poetry install
    ```
4. Active the virtual env
    ```
    poetry shell
    ```
